import { ReactElement } from "react";

export type TButton = {
    bgColor?: string;
    color: string;
    size?: string;
    text?: string;
    borderRadius: string;
    width?: string;
    bgHoverColor?: string;
    icon?: ReactElement;
}